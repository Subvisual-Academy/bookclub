<p>
  <strong>Image:</strong>
  <% if @book.image %>
    <img src="<%= @book.image %>"  alt="book image"/>
  <% else %>
    <img src="https://www.ajnorfield.com/wp-content/uploads/2018/03/question_mark-book-cover.jpg" alt="unknown book cover"/>
  <% end %>
</p>

<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>Author:</strong>
  <%= @book.author %>
</p>

<p>
  <strong>Synopsis:</strong>
  <%= @book.synopsis || "Unavailable" %>
</p>

<p>
  <strong>Isbn:</strong>
  <%= @book.isbn %>
</p>

<p> Presented by :</p>

<ul>
  <% @book.users.each do |user| %>
    <li>
      <div><%= user.name %></div>
    </li>
  <% end %>
</ul>

<%= link_to 'Back', books_path %>

<% if current_user %>
  | <%= link_to 'Edit', edit_book_path(@book) %>
  | <%= link_to 'Destroy', @book, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
