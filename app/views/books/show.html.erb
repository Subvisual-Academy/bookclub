<p>
  <strong>Image:</strong>
  <% if @book.image%>
    <img src="<%= @book.image %>"  alt="book cover"/>
  <% else %>
    <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/d/d2/Question_mark.svg/585px-Question_mark.svg.png"  alt="unknown book cover"/>
  <% end %>
</p>

<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>Synopsis:</strong>
  <%= @book.author || "Unavailable" %>
</p>

<p>
  <strong>Synopsis:</strong>
  <%= @book.synopsis || "Unavailable" %>
</p>


<p> Presented by :</p>

<ul>
  <% @book.users.each do |user| %>
    <li>
      <div><%= user.name %></div>
    </li>
  <% end %>
</ul>

<%= link_to 'Back', books_path %>
